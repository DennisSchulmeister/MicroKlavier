Tests, Experiments and Benchmarks for regular Python
====================================================

This directory contains the experiments that explicitly need to run on
on a regular Python runtime like CPython.